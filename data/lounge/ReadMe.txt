Depth images are 16-bit monochrome images in .png format.
Color images are 8-bit RGB images in .png format.

Images are synchronized and stored in corresponding folders.
Names are the frame number of each image (starting from 1).